<UserControl x:Class="Yomiage.GUI.Views.PresetExternal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Yomiage.GUI.Views"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <!-- 下部ボタン -->
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Height="35" Margin="3">
            <Button Style="{StaticResource IconButton}">
                <DockPanel>
                    <icon:PackIconBoxIcons Kind="RegularPowerOff" Margin="0,0,4,0"/>
                    <TextBlock VerticalAlignment="Bottom">起動</TextBlock>
                </DockPanel>
            </Button>
        </StackPanel>

        <!-- キャラクターリスト -->
        <ListView Margin="0,3,0,0">
            <ListView.Items>
                <sys:String>あああ</sys:String>
                <sys:String>あああ</sys:String>
                <sys:String>あああ</sys:String>
            </ListView.Items>
        </ListView>
    </DockPanel>
</UserControl>
