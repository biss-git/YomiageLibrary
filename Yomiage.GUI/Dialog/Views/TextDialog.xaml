<UserControl x:Class="Yomiage.GUI.Dialog.Views.TextDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Yomiage.GUI.Dialog.Views"
             xmlns:markdig="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom">
            <Separator/>
            <Label Content="ファイルの場所" FontSize="12" Margin="0,0,0,-10"/>
            <TextBox Text="{Binding TextPath.Value}" FontSize="14" MinHeight="10" Background="Transparent" BorderThickness="0"/>
        </StackPanel>
        <ScrollViewer>
            <Grid>
                <markdig:MarkdownViewer Markdown="{Binding MD.Value}" Width="800"/>
                <Label Content="{Binding Text.Value}" Width="800"/>
            </Grid>
        </ScrollViewer>
    </DockPanel>
</UserControl>
